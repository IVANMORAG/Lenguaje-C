// Incluir las bibliotecas estándar necesarias para entrada/salida y funciones generales.
#include <stdio.h>
#include <stdlib.h>

// Función principal del programa.
int main() {
    
    // Imprimir resultados de comparaciones utilizando printf.

    // Imprimir el resultado de la comparación (3 > 5).
    printf(" %d\n", 3 > 5);

    // Imprimir el resultado de la comparación (3 < 5).
    printf(" %d\n", 3 < 5);

    // Imprimir el resultado de la comparación (3 == 5).
    printf(" %d\n", 3 == 5);

    // Imprimir el resultado de la comparación (3 != 5).
    printf(" %d\n", 3 != 5);

    // Imprimir un salto de línea para mayor claridad.
    printf("\n");

    // Indicar que el programa finalizó correctamente devolviendo 0.
    return 0;
}
